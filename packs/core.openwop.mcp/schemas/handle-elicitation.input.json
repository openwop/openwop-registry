{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://packs.openwop.dev/core.openwop.mcp/1.1.3/handle-elicitation.input.json",
  "title": "McpHandleElicitationInput",
  "description": "ADR 0553 P2 — `request` is the LEGACY bridge's shape (the inbound 2025-06-18 `elicitation/create` params) and is no longer required: under the current profile the node is an ordinary step of a `tools/call` workflow with no inbound request to carry, and its ask comes from config.",
  "type": "object",
  "properties": {
    "request": {
      "type": "object",
      "additionalProperties": true
    }
  },
  "additionalProperties": false
}
