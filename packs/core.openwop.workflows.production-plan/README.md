# core.openwop.workflows.production-plan

RFC 0013/0133 workflow-chain pack (1 chain) — Migrated from the deprecated builtin (ADR 0472 P4). chainId keeps the original id; outputRoles restored at registration.

Source of truth: openwop-app repo, `examples/workflow-chain-packs/production-plan/` (ADR 0472 builtin→chain migration).

Chains:

- `openwop-app.production.plan` v1.0.0 — openwop-app.production.plan
