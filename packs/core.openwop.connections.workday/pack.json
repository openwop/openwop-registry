{
  "name": "core.openwop.connections.workday",
  "version": "1.0.0",
  "kind": "connection",
  "description": "Workday connection pack (RFC 0095) — OAuth2 provider for the Workday REST API (HCM / People). DEPLOY-GATED. Reach is `openapi`: Workday has no first-party global MCP server, and its OAuth authorize/token endpoints plus REST base are TENANT-SPECIFIC (`https://{instance}.workday.com/{tenant}/..`), supplied per connection via instanceUrlTemplate. The manifest declares the global https hosts and the per-tenant template; the operator fills the tenant at connection time (capability honesty). Carries NO secret; the operator supplies the API-client id/secret host-side.",
  "license": "Apache-2.0",
  "engines": { "openwop": ">=1.0.0" },
  "provider": {
    "id": "workday",
    "displayName": "Workday",
    "category": "hr",
    "auth": {
      "kind": "oauth2",
      "authFlow": "pkce",
      "scopeModel": "groups",
      "instanceUrlTemplate": "https://{instance}.workday.com/{tenant}",
      "scopes": {
        "read": [
          { "key": "integration", "label": "Integration (read worker/HCM data)", "scopes": ["openid", "offline_access"] }
        ],
        "write": [
          { "key": "staffing.write", "label": "Staffing + HCM (write)", "scopes": ["openid", "offline_access"] }
        ]
      }
    },
    "reach": { "openapi": { "ref": "https://community.workday.com/sites/default/files/file-hosting/restapi/index.html" } },
    "consumerNodes": ["core.openwop.http"],
    "docsUrl": "https://community.workday.com/rest-api"
  }
}
