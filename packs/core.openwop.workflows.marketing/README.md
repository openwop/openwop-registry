# core.openwop.workflows.marketing

RFC 0013 workflow-chain pack (ADR 0149). See the pack manifest for the chains, their connection bindings, and approval gates. Installable on a host via OPENWOP_INSTALL_PACKS.
