{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://packs.openwop.dev/vendor.myndhyve.ai/1.0.0/generateFromPrompt.output.json",
  "title": "GenerateFromPromptOutput",
  "type": "object",
  "required": ["envelopeType", "payload"],
  "properties": {
    "envelopeType": { "type": "string" },
    "payload": { "description": "Envelope payload — shape is envelopeType-specific (per host's envelope normalizer registry)." },
    "envelopeId": { "type": "string", "description": "Host-assigned envelope identifier — surfaces for downstream artifact-sync correlation." },
    "usage": {
      "type": "object",
      "properties": {
        "inputTokens": { "type": "integer", "minimum": 0 },
        "outputTokens": { "type": "integer", "minimum": 0 }
      },
      "additionalProperties": true
    },
    "model": { "type": "string" }
  },
  "additionalProperties": false
}
