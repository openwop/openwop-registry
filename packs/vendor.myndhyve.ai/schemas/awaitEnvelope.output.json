{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://packs.openwop.dev/vendor.myndhyve.ai/1.0.0/awaitEnvelope.output.json",
  "title": "AwaitEnvelopeOutput",
  "type": "object",
  "required": ["envelopeType"],
  "properties": {
    "envelopeType": { "type": "string" },
    "payload": { "description": "Envelope payload (envelopeType-specific)." },
    "envelopeId": { "type": "string" },
    "timedOut": { "type": "boolean", "description": "true if the await resolved via timeout; payload is absent in that case." }
  },
  "additionalProperties": false
}
