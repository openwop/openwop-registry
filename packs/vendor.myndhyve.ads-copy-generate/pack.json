{
  "name": "vendor.myndhyve.ads-copy-generate",
  "version": "1.0.0",
  "description": "MyndHyve ads.copy.generate — AI-powered multi-variant ad copy generation with per-placement adaptation (truncation to caller-supplied text limits). Routes the AI call through ctx.callAI; replaces the source PlatformSpecRegistry read with caller-supplied inputs.placementTextLimits (resolve upstream from vendor.myndhyve.ads-platforms OR host.adPlatformSpecs).",
  "author": "MyndHyve",
  "license": "Apache-2.0",
  "homepage": "https://myndhyve.ai/packs/ads-copy-generate",
  "repository": "https://github.com/openwop/openwop",
  "keywords": ["vendor", "myndhyve", "ads", "copy", "generation", "ai"],
  "engines": {
    "openwop": ">=1.0.0 <2.0.0"
  },
  "peerDependencies": {
    "aiProviders": "supported"
  },
  "runtime": {
    "language": "javascript",
    "format": "esm",
    "entry": "./index.mjs",
    "minRuntimeVersion": "node>=20",
    "requires": ["crypto"]
  },
  "nodes": [
    {
      "typeId": "ads.copy.generate",
      "version": "1.0.0",
      "label": "Generate Ad Copy",
      "description": "Generate N multi-angle ad-copy variants (default 3, max 10) from a brief + placement list. Returns variants with platform-adapted text (truncation when inputs.placementTextLimits is supplied). Falls back to a single-variant heuristic when the AI returns unparseable content.",
      "category": "data",
      "role": "action",
      "capabilities": ["side-effectful"],
      "configSchemaRef": "schemas/copy-generate.config.json",
      "inputSchemaRef": "schemas/copy-generate.input.json",
      "outputSchemaRef": "schemas/copy-generate.output.json"
    }
  ]
}
