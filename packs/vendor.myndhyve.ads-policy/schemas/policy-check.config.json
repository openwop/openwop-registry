{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://packs.openwop.dev/vendor.myndhyve.ads-policy/1.0.0/policy-check.config.json",
  "title": "AdsPolicyCheckConfig",
  "type": "object",
  "properties": {
    "customRules": {
      "type": "array",
      "items": { "type": "object", "additionalProperties": true },
      "description": "Optional caller-supplied rules to append to the 7 built-ins. Same shape: { id, name, category, severity ('low'|'medium'|'high'|'critical'), platforms[], patterns[], keywords[], recommendation, enabled, autoResolvable }."
    }
  },
  "additionalProperties": true
}
