{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://packs.openwop.dev/core.openwop.http/2.1.1/openapi-call.config.json",
  "title": "OpenapiCallConfig",
  "type": "object",
  "required": [
    "operationId"
  ],
  "properties": {
    "operationId": {
      "type": "string"
    },
    "baseUrl": {
      "type": "string",
      "format": "uri"
    },
    "connectionRef": {
      "type": "string",
      "description": "Connection this call requires. Read by the chain loader for pre-flight requirements (chainRequirements) and rendered as a Connect chip in the builder \u2014 so the key IS honoured, just not by the node itself. Declared per ADR 0525 (GRD-8)."
    }
  },
  "additionalProperties": false
}
