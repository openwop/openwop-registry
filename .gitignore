node_modules/
*.log
.DS_Store

# Build + deploy artifacts (regenerated; never committed)
# dist/   — staging output of scripts/build-pack-tarball.mjs; the PUBLISHED
#           artifacts live in registry/, which firebase.json serves.
# .firebase/ — Firebase CLI deploy cache.
dist/
.firebase/
